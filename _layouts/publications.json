---
layout: null
---
[
  {% for pub in site.publications %
  }
    {
    "filename": "{{ pub.url | split: '/' | last | replace: '.md', '.html' }}",
    "type": "{{ pub.type }}"
  }{% if forloop.last == false %
  },
  {% endif %
  }
  {% endfor %
  }
]